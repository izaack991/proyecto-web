function finish(){
    function serie_1() 
    {
    const inputNumeros = document.getElementsByClassName("form-select 1");
    // Inicializamos la variable suma en 0
    let suma = 0;
    // Iteramos sobre los input y sumamos sus valores
    for (let i = 0; i < inputNumeros.length; i++) {
      suma += Number(inputNumeros[i].value);
    }
    // Mostramos la suma en la página
    document.getElementById("resultado1").value = suma;
    }
    function serie_2() 
    {
    const inputNumeros = document.getElementsByClassName("form-select 2");
    // Inicializamos la variable suma en 0
    let suma = 0;
  
    // Iteramos sobre los input y sumamos sus valores
    for (let i = 0; i < inputNumeros.length; i++) {
      suma += Number(inputNumeros[i].value);
    }
    // Mostramos la suma en la página
    document.getElementById("resultado2").value = suma;
    }
    function serie_3() 
    {
    const inputNumeros = document.getElementsByClassName("form-select 3");
    // Inicializamos la variable suma en 0
    let suma = 0;
  
    // Iteramos sobre los input y sumamos sus valores
    for (let i = 0; i < inputNumeros.length; i++) {
      suma += Number(inputNumeros[i].value);
    }
    // Mostramos la suma en la página
    document.getElementById("resultado3").value = suma;
    }
    function serie_4() 
    {
    const inputNumeros = document.getElementsByClassName("form-select 4");
    // Inicializamos la variable suma en 0
    let suma = 0;
  
    // Iteramos sobre los input y sumamos sus valores
    for (let i = 0; i < inputNumeros.length; i++) {
      suma += Number(inputNumeros[i].value);
    }
    // Mostramos la suma en la página
    document.getElementById("resultado4").value = suma;
    }
    function serie_5() 
    {
    const inputNumeros = document.getElementsByClassName("form-select 5");
    // Inicializamos la variable suma en 0
    let suma = 0;
  
    // Iteramos sobre los input y sumamos sus valores
    for (let i = 0; i < inputNumeros.length; i++) {
      suma += Number(inputNumeros[i].value);
    }
    // Mostramos la suma en la página
    document.getElementById("resultado5").value = suma;
    }
    function serie_6() 
    {
    const inputNumeros = document.getElementsByClassName("form-select 6");
    // Inicializamos la variable suma en 0
    let suma = 0;
  
    // Iteramos sobre los input y sumamos sus valores
    for (let i = 0; i < inputNumeros.length; i++) {
      suma += Number(inputNumeros[i].value);
    }
    // Mostramos la suma en la página
    document.getElementById("resultado6").value = suma;
    }
    function serie_7() 
    {
    const inputNumeros = document.getElementsByClassName("form-select 7");
    // Inicializamos la variable suma en 0
    let suma = 0;
  
    // Iteramos sobre los input y sumamos sus valores
    for (let i = 0; i < inputNumeros.length; i++) {
      suma += Number(inputNumeros[i].value);
    }
    // Mostramos la suma en la página
    document.getElementById("resultado7").value = suma;
    }
    function serie_8() 
    {
    const inputNumeros = document.getElementsByClassName("form-select 8");
    // Inicializamos la variable suma en 0
    let suma = 0;
  
    // Iteramos sobre los input y sumamos sus valores
    for (let i = 0; i < inputNumeros.length; i++) {
      suma += Number(inputNumeros[i].value);
    }
    // Mostramos la suma en la página
    document.getElementById("resultado8").value = suma;
    }
    serie_1();
    serie_2();
    serie_3();
    serie_4();
    serie_5();
    serie_6();
    serie_7();
    serie_8();
}
